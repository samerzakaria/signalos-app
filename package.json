{
  "name": "signalos-desktop",
  "version": "1.0.0-beta5",
  "private": true,
  "description": "SignalOS — guided AI workspace for non-technical founders",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "preview": "vite preview"
  },
  "dependencies": {},
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@tauri-apps/api": "^2.0.0"
  }
}
