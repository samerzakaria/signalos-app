id: scenario_136
wave: "W12"
title: "deploy_list returns records"
steps:
  - action: cli
    command: "python3 -c \"import sys; sys.path.insert(0,'cli'); from signalos_lib.deploy import setup_deploy, deploy_list; from pathlib import Path; import tempfile; t=Path(tempfile.mkdtemp()); setup_deploy(t,'12','staging'); setup_deploy(t,'12','production'); records=deploy_list(t,'12'); print(len(records))\""
    expect_exit: 0
    expect_stdout_contains: "2"
