id: scenario_139
wave: "W12"
title: "signal-benchmark CLI works"
steps:
  - action: cli
    command: "python3 -c \"import sys; sys.path.insert(0,'cli'); from signalos_lib.commands.deploy import cmd_signal_benchmark; import tempfile; from pathlib import Path; t=Path(tempfile.mkdtemp()); r=cmd_signal_benchmark(['https://x.com','--wave','12','--repo-root',str(t)]); print(r)\""
    expect_exit: 0
    expect_stdout_contains: "0"
