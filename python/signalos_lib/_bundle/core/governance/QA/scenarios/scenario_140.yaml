id: scenario_140
title: "devex_plan EXPANSION mode produces items"
wave: W13
amd: AMD-CORE-034
command: |
  python3 -c "import sys; sys.path.insert(0,'cli'); from signalos_lib.devex import devex_plan; from pathlib import Path; import tempfile; t=Path(tempfile.mkdtemp()); p=devex_plan(t,'EXPANSION','13'); print(p.mode)"
expect_stdout_contains: "EXPANSION"
