id: scenario_148
title: careful_enable creates flag
wave: W14
amd: AMD-CORE-035
command: python3
args:
  - "-c"
  - "import sys; sys.path.insert(0,'cli'); from signalos_lib.safety import careful_enable; from pathlib import Path; import tempfile; t=Path(tempfile.mkdtemp()); r=careful_enable(t,'deploy'); print(r.active)"
expect_exit: 0
expect_stdout_contains: "True"
