id: scenario_153
title: unfreeze_dir removes freeze
wave: W14
amd: AMD-CORE-035
command: python3
args:
  - "-c"
  - "import sys; sys.path.insert(0,'cli'); from signalos_lib.safety import freeze_dir, unfreeze_dir, guard_check; from pathlib import Path; import tempfile; t=Path(tempfile.mkdtemp()); freeze_dir(t,'src/api','14'); unfreeze_dir(t,'src/api'); result=guard_check(t,'src/api'); print(result['frozen'])"
expect_exit: 0
expect_stdout_contains: "False"
