id: scenario_135
wave: "W12"
title: "record_benchmark stores metrics"
steps:
  - action: cli
    command: "python3 -c \"import sys; sys.path.insert(0,'cli'); from signalos_lib.deploy import record_benchmark; from pathlib import Path; import tempfile; t=Path(tempfile.mkdtemp()); r=record_benchmark(t,'https://example.com','12',1200.0,150.0,0.05,300.0,450.0); print(r.id)\""
    expect_exit: 0
    expect_stdout_contains: "bench-001"
