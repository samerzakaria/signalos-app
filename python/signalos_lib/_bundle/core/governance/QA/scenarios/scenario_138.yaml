id: scenario_138
wave: "W12"
title: "signal-setup-deploy CLI works"
steps:
  - action: cli
    command: "python3 -c \"import sys; sys.path.insert(0,'cli'); from signalos_lib.commands.deploy import cmd_signal_setup_deploy; import tempfile; from pathlib import Path; t=Path(tempfile.mkdtemp()); r=cmd_signal_setup_deploy(['12','staging','--repo-root',str(t)]); print(r)\""
    expect_exit: 0
    expect_stdout_contains: "0"
