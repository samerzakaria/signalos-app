id: scenario_122
wave: "W10"
title: "signal-cso inject-scan returns empty list for a safe file"
steps:
  - action: cli
    command: "python3 -c \"import sys; sys.path.insert(0,'cli'); from signalos_lib.security import scan_injection_risks; from pathlib import Path; import tempfile; t=tempfile.mkdtemp(); f=Path(t)/'safe.py'; f.write_text('x = 1 + 1\n'); r=scan_injection_risks(Path(t), str(f)); print(len(r))\""
    expect_exit: 0
    expect_stdout_contains: "0"
