id: scenario_149
title: careful_status returns active
wave: W14
amd: AMD-CORE-035
command: python3
args:
  - "-c"
  - "import sys; sys.path.insert(0,'cli'); from signalos_lib.safety import careful_enable, careful_status; from pathlib import Path; import tempfile; t=Path(tempfile.mkdtemp()); careful_enable(t); s=careful_status(t); print(s.active)"
expect_exit: 0
expect_stdout_contains: "True"
