id: scenario_145
title: "retro_global returns empty when no brain"
wave: W13
amd: AMD-CORE-034
command: |
  python3 -c "import sys; sys.path.insert(0,'cli'); from signalos_lib.devex import retro_global; from pathlib import Path; import tempfile; t=Path(tempfile.mkdtemp()); result=retro_global(t,'auth','13'); print(len(result))"
expect_stdout_contains: "0"
