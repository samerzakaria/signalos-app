id: scenario_124
wave: "W11"
title: "signal-autoplan generate creates structured task list"
steps:
  - action: cli
    command: "python3 -c \"import sys; sys.path.insert(0,'cli'); from signalos_lib.velocity import autoplan; from pathlib import Path; import tempfile; t=Path(tempfile.mkdtemp()); tasks=autoplan('Add auth\\nAdd tests', '11', t); print(len(tasks)); print(tasks[0].id)\""
    expect_exit: 0
    expect_stdout_contains: "task-001"
