id: scenario_132
wave: "W12"
title: "setup_deploy creates a record"
steps:
  - action: cli
    command: "python3 -c \"import sys; sys.path.insert(0,'cli'); from signalos_lib.deploy import setup_deploy; from pathlib import Path; import tempfile; t=Path(tempfile.mkdtemp()); r=setup_deploy(t,'12','staging'); print(r.id)\""
    expect_exit: 0
    expect_stdout_contains: "deploy-001"
