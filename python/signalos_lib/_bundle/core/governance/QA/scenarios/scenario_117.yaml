id: scenario_117
wave: "W10"
title: "signal-cso canary plant writes token file with correct keys"
steps:
  - action: cli
    command: "python3 -c \"import sys; sys.path.insert(0,'cli'); from signalos_lib.security import plant_canary_token; from pathlib import Path; import tempfile, json; t=tempfile.mkdtemp(); r=plant_canary_token(Path(t), 'test'); print(sorted(r.keys()))\""
    expect_exit: 0
    expect_stdout_contains: "token"
