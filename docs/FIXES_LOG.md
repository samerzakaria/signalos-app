# Fixes Log

A running ledger of concrete issues fixed (and in progress), so changes are trackable.
Newest work at the top. Status: ✅ fixed & verified · 🔄 in progress · ⏳ planned.

Legend for "Verified": how the fix was proven — `test` (unit/integration), `CI` (green in CI),
`e2e` (real end-to-end run), `repro` (reproduced the failure condition, then confirmed fixed).

---

## Offline product acceptance + release hardening (2026-07-16)

Completed the partially-wired dual-context offline acceptance (funded
`react-vite` build/test + a separate source-blind `oracle-playwright`
Chromium context, each in an attested, network-`none`, digest-pinned
container behind a CONNECT-only registry proxy) and hardened the funded
canary path. Findings below were caught by real-Docker + the funded
preflight — the all-Linux offline/CI suites structurally could not see the
Windows-only ones.

| ID | Issue (root cause) | Fix | Verified |
|---|---|---|---|
| OA-1 | `_strict_artifact_snapshot` false-flagged every file as mutated mid-seal on Windows — `os.scandir` `DirEntry.stat()` reports `st_ino`/`st_dev`==0 there, but the re-stat used `os.stat` | Use `os.stat(follow_symlinks=False)` for the before-stat too (cross-platform, byte-faithful seal) | ✅ test + Windows repro |
| OA-2 | Oracle CONNECT proxy exited on startup → `dependency.proxy.start_failed [proxy-readiness]: container is not running` — entrypoint hardcoded `/usr/local/bin/node`, absent in the Playwright image (node at `/usr/bin/node`) | Reviewed-policy `registryProxy.nodePath` allowlist `{/usr/local/bin/node, /usr/bin/node}`, threaded through the proxy entrypoint, anti-tamper inspect probe, and readiness exec | ✅ real-Docker oracle (offline Chromium, 55s) + policy/allowlist tests |
| OA-3 | Funded-Git disabled-hooks check false-failed on macOS + Windows CI — the test passed an **unresolved** temp path (`/var`→`/private/var`, Windows 8.3 short names) tripping the `lexical != canonical` "not independently owned" guard | Resolve the temp root in the test, matching the production driver (which resolves `runtime_home`) | ✅ CI green mac+win + symlinked-parent repro |
| OA-4 | Funded materialize failed on Windows — `workspace package.json does not match the reviewed scaffold`. `Path.write_text` translated `\n`→`\r\n`, and the broker sha-checks the scaffold `package.json` byte-for-byte against the LF fixture (scaffold sha == fixture-with-CRLF, 762 vs 732 bytes) | All 18 stack-adapter `_write` helpers write LF (`newline="\n"`) — no-op on Linux, deterministic on Windows | ✅ byte-parity regression test + CI |
| OA-5 | Release cut from a **red** commit — tag `v3.3.0-internal.2` sat on a commit whose test-automation failed the release-surface version check | Propagated the version to all 4 release surfaces (landing + 3 docs); re-pointed the tag to the fully-green commit | ✅ release-scripts suite + CI green |
| OA-6 | Funded exec sandbox demanded the materialized dependency mount for **every** command at **every** gate, but G4 owns materialization — so any pre-G4 `run_command` (even `ls`) raised `SandboxUnavailableError` and **aborted the whole delivery**. Confirmed systemic by differential control: deepseek-v4-flash did real G0 work (13 tool calls) and was killed by the same wall gpt-oss-120b hit. Industry check (Claude Code, Codex CLI, SWE-bench, OpenHands): sandboxes enforce boundaries, never project-state preconditions, and command failures return to the model | `funded_dependencies_pending()` (fail-closed: pending only when NO materialization artifact exists) + `_load_dependency_mount` runs the same hardened container without the deps volume pre-materialization; partial/tampered state still refuses; G4 still materializes + strictly verifies before build | ✅ broker + sandbox suites (pristine-runs / partial-fails-closed) + live re-run |
| OA-10 | npm `E502 Bad Gateway` fetching a tarball failed the whole funded run on a transient. Root cause is two-sided and was **indistinguishable**: registry.npmjs.org serves documented 5xx blips, AND our own CONNECT proxy answers an identical `502 Bad Gateway` when its upstream TCP connect fails (`connect-proxy.cjs`). Re-running the same code minutes later sailed past prepare, confirming transient | npm now rides out blips itself: bounded `fetch-retries=5` (factor 2, 10s–120s) added to the sealed installer env — retries hit the SAME pinned proxy/authority and every byte is still lockfile-`sha512`-verified, so no egress widening or content smuggling; a real outage still fails bounded | ✅ installer-env pin test; run 4 npm build recovered on re-run |
| OA-9 | G1 gate un-reviewable: `blocked -- no required artifact was written this run (stale outputs)` even though the agent did real work (35 tool calls, 8 diffs). G1's SIGNING manifest (BELIEF + ROLE_ACTIVATION_CARD) is **drafted by the G0 onboarding agent and signed at G1** (documented draft-then-sign), so the dispatched G1 brainstorm agent never (re)writes a manifest artifact — the strict "a manifest artifact must be freshly written this gate" rule can never pass. Audited all 6 gates: **only G1** is fully carried-forward (G2-G5 each write one of their own manifest artifacts) | `_gate_agent_produced_fresh_work`: a carried-forward gate is reviewable when its dispatched agent freshly authored its own card-declared work product (G1 → `core/strategy/brainstorm/`) with the manifest present as the signed surface; every other gate keeps strict manifest-freshness | ✅ orchestrator regression test (fresh-work vs stale-only; G2 unaffected) + live re-run |
| OA-8 | Two more pre-G4 funded walls behind OA-6, found by canary run 2 + a local real-Docker validation of the exact dispatch path (no spend): (a) `agent_loop`'s per-command belt-and-braces receipt check called the **strict** verifier directly, so the first `run_command` on a pristine workspace still died with "materialized dependency receipt is missing or unreadable"; (b) the hardened argv mounted the vite-cache tmpfs INSIDE `/workspace/node_modules`, which only the dependency volume provides — without it Docker mkdirs on the read-only rootfs and the container dies with exit 125 before the command runs | (a) `verify_funded_dependencies_when_materialized()` — pending→None, partial→strict fail-closed; build-time callers (validation/acceptance/subagent_build) stay unconditionally strict; (b) vite-cache tmpfs now requires `dependency_volume` | ✅ broker/agent-loop/sandbox suites + real-Docker pre-G4 run (`SIGNALOS_PREG4_OK`) |
| OA-7 | Agent-card contracts carried **unsatisfiable/self-contradicting prerequisites** that hand literal models a scripted refusal (observed live: gpt-oss-120b declared the greenfield build "out of scope" at G0). Full audit of all 11 bundled cards: G0 onboarding said "Prerequisites: None" then required a stakeholder transcript that cannot exist greenfield, with greenfield only parenthetical; G5 observability hard-required a Release-agent signal-log that no pre-deploy delivery ever creates; test/review/security required a "Build PR" that a local-release delivery never has. A 17-framework survey (Spec Kit, Superpowers, gstack, BMAD, OpenSpec, Codex, Claude Code, Gemini CLI, Aider, Goose, Cline, OpenCode, OpenHands, SWE-agent, MetaGPT, GPT-Engineer, CrewAI) found no one states prerequisites as prompt-text refusal exits — they are harness/pipeline-enforced — and phase-scoped tools reframe the task per phase | Cards: onboarding treats greenfield as first-class ("a 'build X' founder prompt … is in scope, not a refusal reason"), transcripts conditional; observability makes the signal-log/live-metrics conditional ("Do not refuse for a missing signal-log"); test/review/security accept a local release change-set. plan.md/build.md already carried anti-trap clauses (pattern generalized). Harness: `GATE_TASK_FRAMING` prepends a per-gate one-liner to the founder prompt (G0-G3, G5; G4 keeps its build directive). Off-script reprompts already at industry norm (2, bounded) — unchanged | ✅ prompt-contract card tests + `_gate_message` framing test + live re-run |

## End-to-end governed delivery closure (2026-07-14)

This pass closed the desktop and direct-backend seams together. A delivery is
now project/run-bound from the founder's G0 approval through G4 attribution and
the G5 release receipt; the offline matrix drives the same backend without the
desktop UI.

| Area | Failure closed | Implemented contract | Verification |
|---|---|---|---|
| G0 founder authority | Project creation or a generic verdict could manufacture founder approval | Exact consent (`I approve Gate 0 as sole founder`), explicit button/chat/simulation provenance, workspace + project + approval-ID binding, durable transaction/rollback, project-bound PO authority with scoped PE delegation, and placeholder refusal | G0/IPC/orchestrator integration suites |
| Desktop-to-backend identity | A stale card or global run ID could approve another project/run | Every gate card is bound to workspace, project, run and gate; stale cards retire; in-flight responses cannot overwrite a new project; reopened gates become actionable again | Vitest workspace, chat and agent-event suites |
| Direct backend lifecycle | Scripted tests did not traverse the durable resume/cancel/reopen boundary | Canonical run-ID grammar, containment-safe run storage, single workspace delivery lock, persisted profile/project/signer, active and idle cancellation, one-shot reopen, and pre-provider resume validation | IPC boundary, resume, reopen and direct matrix suites |
| Virtual-project isolation | Identical artifact paths/hashes could replay a global audit row across projects | New sign/reopen/release audit rows carry `project_id`; non-default validation requires an exact match; legacy unbound rows remain compatible only for `default`; registry mutations refuse corrupt or redirected authority files | Cross-project replay and registry tests |
| G4 generated-code proof | A pre-existing or trivial green tree could be treated as this run's build | Versioned run-bound attribution records the pre/post source tree, requires a meaningful product delta, stores real build/test results, and binds the entire shippable payload digest (including tracked build output and deletions) | Attribution and tamper tests |
| G5 release truth | A raw G5 signature, forged trailer, stale marker, or successful-looking push could report shipped | Raw G4/G5 signing is refused; G5 revalidates all gates and the current G4 tree, persists a pending checkpoint before effects, commits through an isolated exact index, rejects non-canonical control drift and redirected paths, verifies the local seal/hash and release commit, then reads the exact pushed SHA back from the remote branch before `ready=true` | Real bare-remote, crash-recovery, cancellation and forged-receipt tests |
| Backend matrix + CI | Browser oracle coverage lived outside the normal CI path | Repository-owned offline driver exercises the complete backend gate walk; clean-room browser oracle validates the generated product; the browser-enabled CI journey job now runs the oracle without API keys or paid providers | 29 backend-matrix/oracle tests; workflow/ACL checks |
| Production desktop profile | Desktop delivery could resume with benchmark-only safety stages or accept raw HTML as UX proof | New desktop deliveries use `production`; the profile is persisted and immutable across resume; G5 independently requires current security, runtime, and an executed post-JavaScript browser receipt for UI products | Delivery-profile, runtime-proof, resume and desktop journey suites |
| Packaged browser proof | Browser proof worked in a source checkout but a packaged desktop could lack Playwright/Chromium and block every UI release | Existing project/source tooling is preferred; otherwise a pinned Playwright + matching Chromium runtime is provisioned once into an OS user-scoped cache outside the product/release tree, with bounded locking/timeouts, stable-browser fallback, redacted diagnostics and fail-closed verification | Runtime-proof bootstrap/discovery/cache/channel tests + live journey |
| Credential boundary | Model-authored commands, preview servers, browser proof, or tooling bootstrap could inherit the sidecar's provider key | Every generated-command, preview, browser and tooling subprocess receives a credential-scrubbed child environment while the trusted provider process retains its key; non-secret PATH/proxy/tool settings remain available | Sandbox, AgentLoop, IPC and runtime-proof credential tests |
| Audit and reversal authority | Deleting a mutable reopen marker or appending a hashless sign row could resurrect an old approval | Reopen/revoke is appended to the chained project-bound audit before the marker; strict validation requires an exact current artifact hash from a valid chained authority row and honors the latest sign/reversal event | Revocation deletion, fresh re-sign, hashless-tail and cross-project tests |

---

## Desktop-journey wave — close fail-open holes in the real desktop flow (2026-07-14)

Five checkpoints (C1–C7) audited against the shipped desktop journey; C5 (G4 this-run attribution) and C7 (G5 verify-before-push) were already closed by the governance epoch (G-3 / G-5). The three real remaining holes:

| # | Hole | Fix | Location | Verified |
|---|---|---|---|---|
| C1 | **G0 auto-signed as the founder at project creation** — New Project signed G0 under PO **and** PE (APPROVED) with no review: a rubber-stamp of the governance agreement (Soul / Constitution / Decision-DNA) the founder never read | Fill the docs but do **not** sign. `instantiateGovernance` (fill-only) + `approveGate0` (explicit sign). G0 signs only on a real human act: the **Approve Gate 0** button or a chat approval ("approve / accepted / signed / agree", tight classifier that excludes questions and build requests). New Project surfaces G0 as awaiting approval, not an error | `services/workspace.ts`, `js/ui/chat.js` (`isApprovalIntent`), `components/ChatBubbleSystem.tsx`, `js/app-v2.js` | ✅ `workspace.test.ts` (no sign at creation; `approveGate0` signs PO+PE), `chat.delivery-intent.test.ts` (approval vs build/question); tsc clean |
| C4 | **A blocked gate was silent / read as accepted in the UI** — the engine emits `gate_blocked` and returns `not-reviewable` (e.g. G4's anti-fake-green wall), but the UI had no handler and `interpretVerdictAck` didn't list `not-reviewable`, so a refused verdict looked signed | Handle the `gate_blocked` event (transcript bubble with the reason) and treat `not-reviewable` / `conditions-need-text` as verdict refusals so the card reverts | `services/agentEvents.ts` | ✅ full vitest suite |
| C6 | **Desktop delivery inherited the lenient `benchmark` profile** — `agent:deliver` built the orchestrator with no profile, so real desktop products skipped the production security gate + runtime-proof evidence | `agent_deliver` passes `profile="production"` (overridable via payload). Hang-safe: the proof stage runs only after `_g4_verify.ok`, so it never serves a stub. Benchmark harness keeps the default by not passing a profile | `signalos_ipc_server.py` | ✅ desktop e2e 5 passed; ipc + orchestrator 107 passed |

---

## Build-time UX/DoD gate cluster (b43a89a) — follow-ups + delivery fail-fast (2026-07-13)

The build-time UX/behavioral acceptance HARD gate (G4) + per-task DoD gate landed at b43a89a ("enforce what you grade"). Two defects surfaced while validating that cluster end-to-end and running the full suite as the integration gate. Full suite **2901 passed / 5 skipped / 37 subtests** (14:26).

| # | Issue | Root cause | Fix | Location | Verified |
|---|---|---|---|---|---|
| U-1 | The auto-authored UX-acceptance test would **fail `tsc --noEmit` in every real build** → build INVALID (would have broken the enforced GPT-5.5 re-run and every enforced build) | The authored `.test.tsx` template had untyped params (`noImplicitAny`), `.textContent.trim()` with no null-guard, and `el.style` on an `Element`-typed var | Typed the template (`collectControls`/`accessibleName`/`.map((id: string)`), null-guarded `textContent`, dropped `el.style` | `product/acceptance.py` `ux_acceptance_test_source()` | ✅ authored into a real build → `tsc --noEmit` **0 errors**; 44 acceptance tests pass |
| U-2 | A **determined-failed delivery still ran the full runtime proof** (spin up dev server + poll health) and a real tsc/vitest validation on the scaffold stub → wasteful in prod, and on a restricted network **hung the whole test suite** (health-poll `socket.connect` never returns → pytest-timeout killed the session) | `run_delivery` PROOF + VALIDATION phases ignored `generation_blocked`; the failed-dispatch path (no files written) reached both | On `generation_blocked`: skip runtime + UX proofs (synthesize `skipped`); skip real tsc/vitest + the repair loop (synthesize failed validation — the `_mark_generation_failed` override already discards the stub's verdict). Fail-fast on a delivery already known failed | `product/delivery.py` (VALIDATION + PROOF phases) | ✅ `test_repair_convergence_honesty.py::…fails_delivery_closed` 80–186s+hang → **9s pass**; full suite green |

**Benchmark durability:** the funded-run harness is now checked into the repo at `scripts/backend_matrix/` (driver + expense-tracker scenario + external browser oracle, honest scope disclaimer, `--acknowledge-key-exposure` gate), replacing the session-temp grader. It never auto-loads the repo `.env`, never puts the key in payloads/args, and runs the final product check clean-room (fresh install + lockfile). Model catalog is OpenRouter aliases only — no hardcoded key, no banned `gpt-4o-mini`.

---

## Durable direction — runtime containment, cassette replay, near-miss tier (2026-07-10)

The endgame layers the panels named: replace static command policy with a real OS boundary, turn funded-run transcripts into a free offline corpus, and restore benchmark signal without re-opening the vacuous-satisfaction trap. Full suite green.

| # | Item | What | Location | Verified | Commit |
|---|------|------|----------|----------|--------|
| 78 | **Runtime containment (boundary endgame)** | Opt-in `ContainerRunner` (docker/podman/wsl) runs commands in a throwaway container — only the workspace bind-mounted rw, `--network none`, cpu/mem/pid caps, env forwarded as an overlay (no host-env leak). In-code path/allowlist policy demoted to a backstop. Default `InProcessRunner` byte-identical. | `product/sandbox.py` (new), `product/agent_loop.py` | test (40+107); **real containment verified on Docker + WSL**: network blocked, host paths absent, env didn't leak, `--read-only` rootfs immutable (only `/workspace` + `/tmp`/`/root` tmpfs writable) | (this push) |
| 79 | **Cassette replay (Layer 2)** | Offline replay of recorded provider payloads through the real litellm/adapter path (`CassetteTransport` at the httpx seam) — tests tool-call parse, reasoning-channel leak, streamed-delta assembly for $0. URL-aware so litellm's cost-map GET doesn't desync (also fixed a pre-existing isolation bug). Capture-OFF byte-identical (+163/−0). | `product/provider_adapter.py`, `test_cassette_replay.py` (new) | test (15+64) | (this push) |
| 80 | **Near-miss grade tier (Fable-5 decision)** | A near-complete-but-non-compiling build no longer scores identically to nothing. Strictly gated (≥5 impl files & ≥150 LOC AND hidden-gold ≥90% AND full-suite ≥90% AND ≤3 tsc errors all unused-symbol family); `min()` ceiling of 50, still letter **F**. Anchored on the un-fakeable hidden-gold oracle. Vacuous/empty and plausible-but-broken builds still floor to 20. | benchmark grader (scratchpad: `measure_360.py`) | test (90); re-grade gpt-oss 20→50/F, glm 92.5/A unchanged | scratchpad |
| — | **Benchmark repair budget** | tightened to 3 cycles (panel: 2–3, and now logged) — benchmark-harness env only; production/desktop keep the default 8 | benchmark harness (`prove_g4.py` env) | — | scratchpad |

---

## Engine profiles + panel-validated service design (2026-07-10)

WS-E's conservative Claim-2 convergence deferred four services (repair/proof/security/review) because turning them on would change benchmark behavior. A cross-vendor panel resolved them via **one engine, config-gated profiles** — benchmark stays deterministic, production adds release-safety. Implemented additively; **benchmark path byte-identical, full suite 2470/0**.

| # | Issue | Root cause | Fix | Location | Verified | Commit |
|---|-------|-----------|-----|----------|----------|--------|
| 75 | One engine must serve both deterministic benchmarking and full production delivery without forking | GateOrchestrator ran neither the security gate nor runtime proof; adding them unconditionally would inject flakiness/score-changes into the benchmark | `profile: benchmark\|production` switch (**default benchmark = no extra stages, proven byte-identical**); production runs security-gate (CRITICAL hard-blocks the sign, fails *open* on a degraded/flaky scanner) + runtime-proof (evidence-only, never blocks) strictly **after** G4 verify, before sign | `product/gate_orchestrator.py` | test (49+50) | (this push) |
| 76 | "Retry until green" could hide a shaky build; per-task repair can't see integration failures | no machine-readable repair metric — "converged in 0 repairs" was indistinguishable from "needed 3"; final integration-repair already existed but was invisible | Per-phase `repair_attempts` counters (per_task / integration / review) surfaced in `traceability.json` + `BUILD_EVIDENCE.md` + `LoopResult`; final integration repair stays **bounded** and never forces green. Green build = zero added work (byte-identical dispatch) | `product/subagent_build.py` | test (38) | (this push) |
| 77 | Review-gate looked like a duplicate of the LLM judge; security double-counted (gate + cap on same severity) | (corrected via real code: `review_gate.py` is DETERMINISTIC, not an LLM — kept) | Judge-as-gate: `judge_gate()` blocks on the **same** median-of-3 result (no second pass); security split — CAP for scoring (CRITICAL 30/HIGH 65/MED 85 preserved), CRITICAL-only `gate_block` for production; never both within a profile. `review_gate.py` untouched | benchmark grader (scratchpad: `cq_judge.py`, `measure_360.py`) | test (78), re-grade unchanged | scratchpad |

---

## Root-cause structural hardening + readiness gate (2026-07-10)

Panels validated that our recurring failures were a *class*: guaranteeing properties with growing heuristic lists (command/path allowlists, a post-hoc wiring reviewer) and mocked test seams, instead of structural boundaries + executable proofs. This wave fixes the *class*, and adds the deterministic readiness gate so we stop discovering harness bugs by burning funded builds. Integrated: **2458 passed, 0 failed**, readiness gate green.

| # | Issue | Root cause | Fix | Location | Verified | Commit |
|---|-------|-----------|-----|----------|----------|--------|
| 70 | **Endless command/path allowlist false-denials** (`cd /c/ws && npm test` denied as "outside workspace"; absolute write to an allowlisted dir denied; `node -e`/`sha256sum` gaps) | OS-sandbox-by-regex: string-matched command prefixes + path globs against whatever *spelling* the model typed (Win `c:\` vs Git-Bash `/c/` vs quoted vs absolute) | ONE canonical path pipeline (`_degitbash`/`_workspace_relative`) feeding every containment + allowlist check (canonicalize→relative→glob); cwd **jailed** to workspace so no `cd <abs>` needed; verification-command **class** instead of enumerating | `product/agent_loop.py` | test (134, +21), repro | `33da5ef` |
| 71 | **Modules generated but never wired**; UX must-haves asked in prose, never enforced | reachability checked out-of-band by a reactive LLM reviewer; "done" = file exists, not app renders it | **Deleted the wiring reviewer** (→ advisory lint); reachability + responsive/empty/loading/error promoted to **RED-gated acceptance criteria** (behavioral `render(<App/>)` tests); G4 consumes canonical `PLAN.tasks.yaml` | `product/subagent_build.py`, `acceptance.py`, `wiring_check.py`, bundled agent prompts | test (97) | `ccdb9fc` |
| 72 | **Harness bugs only discovered by burning funded LLM builds** ($0.80/55min each) | validated the harness with a green *unit* suite that mocks the seams where the bugs live | **Layered readiness gate** — capability-contract unit tests (regression-proven: reverting the fix → 22 fails; real litellm still false-negatives all pinned IDs), **wire-level** golden path (fake at the HTTP boundary, real litellm path), opt-in raw-transcript capture (+151/−0, off by default) | `test_capability_contract.py`, `test_golden_path_e2e.py`, `test_transcript_capture.py`, `product/provider_adapter.py` | test (55, $0/sec) | `7cabb3a` |
| 73 | **Two diverged engines** (Claim 2) — GateOrchestrator path didn't share `run_delivery`'s services | historical fork; validation already shared, closeout/repair/proof/security/review not called | Conservative convergence: **locked** the shared validation with a test; wired **closeout** via the same impl (strictly post-G5, `.signalos`-only, opt-out) — benchmark path proven byte-identical. Repair/proof/security/review deferred as profile design decisions (panel-guided) | `product/gate_orchestrator.py` | test (42), repro | `f200bb5` |
| 74 | **Grader penalized capable models for OUR bugs** (glm `clean_walk` 0 = 92% harness denials; `cn()`-composed UX undercounted; `lines≤30` over-penalized React) | conflated harness-caused vs model-caused; literal `className` regex; flat per-threshold deduction | Split `clean_walk` **harness-vs-policy** (only policy scores the model; still catches qwen's signed-test tampering); `cn()`/`clsx` class extraction; **graduated** context-aware complexity; `failure_taxonomy` in the 360 | benchmark grader (scratchpad: `gov_v2.py`, `cq_ux.py`, `cq_deterministic.py`, `measure_360.py`) | test (48), re-grade | scratchpad |

---

## Foundry governed-build hardening — audit-verified (2026-07-10)

An on-disk audit confirmed 11/11 P0/P1 findings a consultant raised: the visible Foundry app
+ the benchmark harness could report "no generated code" before model quality mattered. Six
parallel workstreams fixed them; findings were separated into *benchmark-path* (what scores
the models) vs *desktop-path* (what a real user hits). A cross-vendor panel ($0.26) ranked the
tool-call fix. Offline suite after: **2363 passed, 0 failed**.

| # | Issue | Root cause | Fix | Location | Verified | Commit |
|---|-------|-----------|-----|----------|----------|--------|
| 54 | **Capable models (glm/qwen) scored as producing nothing** — narrate a plan, emit no tool call, run deadlocks | `stop_reason != "tool_use"` treated a no-tool-call turn (and `max_tokens` truncation) as `completed`; the subagent wrapper discarded `LoopResult.status` | No-tool turn in a work-expecting context is re-prompted (then guarded `tool_choice="required"` with auto-fallback); truncation is continued; a stalled run returns `stalled_no_tool`, never `completed` | `product/agent_loop.py`, `provider_adapter.py`, `subagent_build.py` | test, repro | `30bb4bb` |
| 55 | Forced `required` could write a placeholder (empty path/content) that looks like success | tool-call presence was trusted without validating args | `_tool_call_defect` rejects empty/placeholder/unknown calls; they don't count as work, nothing is written | `product/agent_loop.py` | test | `30bb4bb` |
| 56 | `cd frontend && npm test` denied while bare `npm test` allowed → false trust-tier denials tanked governance score | command matcher only checked the first two tokens | Validate each `&&`/`;`/`\|\|` segment; allow a leading in-workspace `cd` | `product/agent_loop.py` | test | `30bb4bb` |
| 57 | Non-React task declared "green" without running any test (fake-green) | `_run_single_test` returned `(True,"")` when a stack had no per-test runner; caller skipped the implementer as pre-existing-green | No runner → honest `(False, …)`; implementer runs; full-suite gate remains the real check | `product/subagent_build.py` | test, repro | `30bb4bb` |
| 58 | Model denied writing stack files it must create (`index.html`, `vite.config.ts`, `pyproject.toml`, `Cargo.toml`, `go.mod`, Next `app/**`) → deadlock-by-policy | T2 write/execute allowlist omitted real adapter files/commands | Allowlist extended to what the shipped adapters actually create/run (no `**`) | `product/enforcement_state.py` | test | `30bb4bb` |
| 59 | Gate read "passed" on **any** signed artifact while G4 preflight required **all** → dead-end projects | `status._detect_gates` used `any()` | Fail-closed `all()` over the required manifest (matches preflight); gate-seed fixtures now sign every artifact | `status.py`, `conftest.py`, 6 wave tests | test | `30bb4bb` |
| 60 | Selected stack lost before G4 (react → detected `generic` → Python cmds); greenfield deadlocked with no shell | `detect_profile` ignored `.signalos/profile.json`; GateOrchestrator never scaffolded | Honor `profile.json`; idempotent scaffold-first before G4 (no-op when shell exists — benchmark-safe, sha256-proven) | `product/gate_orchestrator.py`, `stacks.py` | test | `30bb4bb` |
| 61 | "Create an authentication module" / "Add login" / any `?`-ending prompt could not build | intent classifier omitted module/library/auth/… and forced every `?` to conversation mode (writes refused) | Broadened vocabulary; dropped the blanket `?`→conversational rule | `src/js/ui/chat.js` | test, build | `f6f6e1a` |
| 62 | Gate verdict optimistic + could advance the wrong run; a failure stranded the card | fire-and-forget send using a stale global `lastRunId`; no revert path | Verdict carries the bubble's own run_id, awaited + revertible (refusal re-opens the card) | `BuildView.tsx`, `agentEvents.ts` | test, build | `f6f6e1a` |
| 63 | G4 progress silent; generated files unbrowsable; dep auto-repair a silent no-op; deliver killed at 10 min | dropped `system`/`gate_signed`/`delivery_complete` events; single-level tree, no expand; `read_workspace_file` passed `path` not `relative_path`; hard 600 s cap | Render those events; expandable lazy file tree; fix arg to `relative_path`; deliver uncapped | `agentEvents.ts`, `Sidebar.tsx`, `preview.ts`, `approvePlan.ts`, `chat.js`, `ipc.rs` | test, build | `f6f6e1a` |
| 64 | Shipped sidecar could be stale (`0.0.9`, no `agent:deliver`) yet accepted → desktop can't generate | `ensure-sidecar` checked existence only; no runtime capability check | Startup capability handshake (fails loud if `agent:deliver` absent); freshness-guarding ensure scripts (+`.ps1`) | `src-tauri/src/sidecar.rs`, `signalos_ipc_server.py`, `scripts/ensure-sidecar.{sh,ps1}` | test (cargo), repro | `f6f6e1a` |
| 65 | `agent:cancel` couldn't interrupt an in-flight delivery; nested dirs unlistable | serial stdin loop; single-level `list_workspace_dir` | Dedicated stdin reader honors cancel mid-flight; `capabilities` command; per-level dir listing with containment | `signalos_ipc_server.py`, `src-tauri/src/ipc.rs` | test (cargo) | `f6f6e1a` |
| 67 | **Capable OpenRouter models (glm-5.2, qwen3.7-max, gpt-oss-120b) offered NO tools → could only narrate → scored as producing nothing** | `detect_capabilities` trusted `litellm.supports_function_calling`, whose static registry false-negatives on new models (returns `False` though OpenRouter reports `tools=True`) → `supports_tool_calls=False` → agent loop ran tool-less → `text_only` narration, no build. (The Claim-54 reprompt path lives in the with-tools branch, so it never ran.) | Trust a litellm `True`; treat `False`/unknown as unknown and defer to the name heuristic (tools-on unless the name marks it tool-less) | `product/provider_adapter.py` | test, repro | `0cca730` |
| 68 | **Fresh-project onboarding dead-ends + G0 signing failed invisibly** | UI signed G0 fire-and-forget (reported success before the sidecar finished) using `--verdict pass` which the CLI rejects → sign failed silently, and with a single role though G0 needs PO+PE; init never seeded Surface Inventory / Permanently-T3 / BELIEF / ROLE_ACTIVATION_CARD, so G1's brainstorm agent dead-ended | `workspace.ts` awaits `signal.runAndWait`, uses `APPROVED`, signs once per required role; `init.py` seeds the 2 missing G0 artifacts + DRAFT G1 Belief/Role-Card (never auto-signed — G1 stays open) | `src/services/workspace.ts`, `commands/init.py` | test, e2e-smoke | `3406a5b` |
| 69 | **Stale sidecar binary rebuilt** (closes Claim 1); ps1 freshness guard false-negatived a *good* binary | binary was 0.0.9 (no `agent:deliver`); the ps1 probe's stdin carried a UTF-8 BOM that decoded to garbage under the sidecar's cp1252 stdin | rebuilt via PyInstaller (now 3.3.0-internal.1, serves `agent:deliver`, 17→73 MB); probe forces BOM-less UTF-8 | `scripts/ensure-sidecar.ps1` + bin rebuild | test, repro | `3406a5b` |
| 66 | Offline test suite non-hermetic → slow, order-dependent, non-deterministic failures | `test_postgres_task_store._load_dotenv` loads a repo-root `.env` into `os.environ`; conftest's hermetic clear-list **omitted `OPENROUTER_API_KEY`**, leaking a live key so downstream `run_delivery` routed to a real provider. Pre-existing (pure HEAD + `.env` reproduces) | Added `OPENROUTER_API_KEY` (+`_API_BASE`/CEREBRAS/DASHSCOPE) to the hermetic clear-list | `conftest.py` | test, repro | `30bb4bb` |

Deferred (documented, not benchmark-affecting): canonical `PLAN.tasks.yaml` → G4 markdown-parser unification (latent architecture split); actual sidecar `.exe` PyInstaller rebuild (must run on a build machine/CI — the new handshake+freshness guard fail loud until then).

---

## Real user-journey / cockpit UX — the layer that was never e2e-tested (v3.2.4-internal.2 + follow-up)

Every UI unit test mocks the Tauri bridge, so nothing ever walked the real cockpit — journey-breaking
bugs shipped "green". Found by direct user reports on the installed app + a new Playwright journey smoke
(`e2e/user-journey.smoke.mjs`).

| # | Issue | Root cause | Fix | Location | Verified | Commit |
|---|-------|-----------|-----|----------|----------|--------|
| 50 | **Chat wiped on tab switch** — moving away from Build and back showed a brand-new chat with no history, even mid-task | `loadBuild()` runs on *every* switch to Build and unconditionally overwrote `state.chatBubbles`, rebuilding from only persisted turns → a live/unpersisted conversation was replaced by just the welcome bubble | Only **hydrate when the chat is empty**; never clobber a live/loaded conversation on re-entry | `src/js/ui/chat.js` | test (2), build | `8d33c19` |
| 51 | **"Picked a folder, still 'Choose a projects root'"** — onboarding dead-ended; Browse never registered the folder | `browseProjectsRoot` used `window.__TAURI__.dialog`, usually **undefined** under `withGlobalTauri` in Tauri v2 → silently fell through to `window.prompt` (a no-op in the webview) | Use the reliable `@tauri-apps/plugin-dialog` binding (same pattern chat.js uses), with global + prompt as ordered fallbacks | `src/components/Onboarding.tsx` | test, build | `8d33c19` |
| 52 | **Models sometimes not fetched → nothing works (no model selectable)** | `fetch_provider_models` can fire before the sidecar picked up the freshly-stored key / finished starting; a single attempt returned empty/errored with no retry | Retry transient failures **and empty results** (3×, backoff); auth failures still fail fast so a bad key is cleared | `src/services/providerModels.ts` | test (2), build | `8d33c19` |
| 53 | **Fresh install → "Agent run failed: Build precheck failed: No workspace selected"** — new install skipped onboarding, landed with `Active folder: (none)`, and every build died in the Rust precheck | Two-part: (a) WebView2 `localStorage` persists across reinstalls → the `signalos.onboarding.wizard.v1` "done" flag survived → onboarding skipped; (b) onboarding **by design** only sets the projects *root* (workspace stays `(none)` until a project is created) but nothing guided the user there — the first delivery fired straight into `enforcement.rs` `"No workspace selected"`. Hit **every** first-time user, not just reinstalls. | (a) `isFinished()` now also requires a real persisted `projectsRoot` (corrupt/partial "done" re-shows onboarding); (b) a **workspace guard** in the delivery path: no active workspace → plain-language system bubble + open **New Project** instead of firing a doomed build; plus a catch-net that re-routes any `"no workspace selected"` surfacing from the sidecar | `src/js/wizard.js`, `src/js/ui/chat.js` | test (3), repro, build | `fab2136` |

---

## Release v3.2.3-internal.1 — supply-chain, CI, and executor fixes (2026-07-03)

| # | Issue | Root cause | Fix | Location | Verified | Commit |
|---|-------|-----------|-----|----------|----------|--------|
| 1 | **quick-xml RUSTSEC-2026-0194/0195** (2× high, DoS) failing `cargo audit` — blocked the Release + nightly | Two advisories published against transitive `quick-xml`; not our code | Removed the `quick-xml 0.37.5` instance by updating `notify-rust` 4.17→4.18 and `tauri-winrt-notification` 0.7.2→0.7.3 (drops quick-xml from the Windows-notification path). **Real removal, not an audit ignore.** | `src-tauri/Cargo.lock` | audit (4→2), `cargo check` | `69fbc20` |
| 2 | Remaining `quick-xml 0.39.4` advisories (via `plist`, pinned `^0.39.2`, no released fix) | `plist` 1.9.0 can't take the fixed quick-xml 0.41.0 | Pointed `plist` at the **real upstream fix** (PR `ebarnard/rust-plist#191`, pinned to an immutable commit) via `[patch.crates-io]` — reviewed it (pure version bump). **Not a suppression.** Tracked for removal when plist releases. | `src-tauri/Cargo.toml`, `Cargo.lock` | audit (2→0), `cargo check`, **CI** | `4637959` |
| 3 | **Executor git-identity** — parallel executor's git commits/merges failed → delivery silently produced no files (missing `src/types.ts`) | Commits relied on an ambient `git user.name/email`; a fresh scaffold sets none, and CI runners / user machines may have none | Inject explicit `-c user.email -c user.name -c commit.gpgsign=false` on every git call. **Real app bug** (not just CI). | `python/signalos_lib/product/executor.py` | repro (`GIT_CONFIG_GLOBAL=/dev/null`), test (44), **CI** | `6b521ed` |
| 4 | **macOS-only test false-negative** — `test_agent_launch_walks_a_real_gate…` failed only on macOS | `os.getcwd()` resolves `/var`→`/private/var` on macOS; raw-string `startswith` broke | Compare **resolved** paths via `is_relative_to`. Test-only; no app change. | `python/test_ipc_agent_launch.py` | **CI** (all 3 platforms) | `211a2e4` |
| 5 | **Unborn-branch** — `run_isolated_build_tasks` crashed on a freshly-scaffolded repo | Fresh `git init` repo has zero commits; `git worktree add` can't fork from an unborn branch | `_ensure_has_commit()` makes a base commit before worktree ops; read branch via `symbolic-ref` (works pre-commit) | `python/signalos_lib/product/executor.py` | test, repro | `ba9a3da` |
| 6 | **`.signalos/` leaked into merges** — parallel build produced 9 `agent-runs/` entries instead of 1 | Each worktree commit staged `.signalos/` (SignalOS's own bookkeeping) | Exclude `.signalos/` from task commits via git pathspec (`-- . ':!.signalos'`) | `python/signalos_lib/product/executor.py` | test, repro | `ba9a3da` |

---

## Generation quality — the "shity output" root causes (via workflow `wf_866b63df-114`)

Discovered by running a **real end-to-end generation** (not just tests) as Mester Tester (PO).
Verify agent ran a **real Anthropic generation** of the expense tracker: 11/11 files, functional
component confirmed, 44 Foundry suites pass — but the generated app did **not build** (cross-file
drift, #12). Truncation/shells/blueprint/perf fixed; cohesion is the remaining half.

| # | Issue | Root cause | Fix | Verified | Status |
|---|-------|-----------|-----|----------|--------|
| 7 | **LLM generation truncates** — asked for 18 files, got 2 → broken products | `dispatch_build_agent` fired **one** call ("Output ALL files in one response") capped at **`max_tokens=1024`**; flattened the plan's small linked tasks into a monolithic dump | Per-file/per-task concurrent LLM calls via the worker pool, per-file token budget (8K–128K), truncation now a retryable signal (`TruncatedResponseError`) | e2e: **11/11 files, valid=true, no truncation** | ✅ |
| 8 | **Components are non-functional shells** | `_render_component` emitted a static "metric card" (`Ready for validation`) — no state/handlers/CRUD | Per-file prompt demands **functional** components (zustand/hooks state, handlers, add/edit/delete) + a real **interaction test** | e2e: `Expense.tsx` has real store+CRUD+total+breakdown; test uses fireEvent | ✅ |
| 9 | **Blueprint domain mismatch** — "expense tracker" → metrics/revenue dashboard | `match_blueprint` scoring picked wrong domain; **and** LLM `product_type` mis-classification (`financial-dashboard`) overrode correctly-extracted Expense/Category entities | Fuzzy blueprint matches require a confidence floor; LLM-refined exact `product_type` labels now need entity/keyword corroboration against the **deterministic** extraction (not the LLM-rewritten entities) before selecting a built-in blueprint. A misclassified expense tracker falls back to generic instead of the revenue dashboard; real revenue/churn intents still match financial-dashboard | **REAL Anthropic e2e (`wf_7f6efd7f`)**: the expense prompt generated an actual expense tracker (`Expense.tsx`+`Category.tsx`; add/total/per-category/reimburse), `DELIVERY_STATE.blueprint=''` — did NOT snap to financial-dashboard; test (`test_blueprint_match_threshold.py`), 33 threshold/cohesion tests | ✅ |
| 10 | **Parallel executor too slow on Windows** — 7-comp app timed out (>3min), left worktrees | `run_isolated_build_tasks` uses git-worktree-per-task + merge queue (heavy on Windows) | Added `run_inprocess_build_tasks` (git-free fast path); worktree isolation now **opt-in** | test (`test_parallel_no_worktrees`), 44 suites pass | ✅ |
| 11 | **Generated UI tests are render-smoke only** | Components had no behavior (see #8) | Per-file test prompt demands a real interaction (render→type→submit→assert) | e2e: interaction test generated (but see #12) | ✅ (gen) / 🔄 (compiles) |
| 12 | **Cross-file generation drift — app doesn't build** (the "keep connected" half) | Each file generated in **isolation** → inconsistent names/types: no shared `src/types.ts`; `App.tsx` imports `ExpenseManager` but file is `Expense.tsx`; test imports non-existent modules; field `isReimbursed` vs `reimbursed`; no vitest jest-dom setup | Shared authoritative contract across chunks: `types.ts` always emitted + injected; canonical component manifest used by `App.tsx` + tests; shared field names; vitest setup; `check_cross_file_consistency` validator | **Partial** — contract works: `App.tsx` imports the exact generated components, `types.ts`/`setup.ts` resolve, fields shared (imports_resolve broken→partial). **Still RED** (86 TS errors): drift moved to a new layer (see #22) | 🔄 |
| 22 | **Drift layer 2 + compile-feedback repair loop** | LLM invents modules/aliases + writes code inconsistent with its own types | **Repair loop BUILT + works**: real `tsc && vite build` + `vitest` in validation, structured per-file errors fed back, only failing files regenerated (bounded). Verified live: errors shrank 31→21→20→19. **But does NOT fully converge (see #24).** First-pass generation is genuinely functional (Expense.tsx 19.7KB real CRUD) | e2e: loop runs + shrinks errors; build still red | 🔄 (partial — see #24) |
| 23 | **FAKE-GREEN in the pipeline: generation failure reported as build passed** (found by the repair e2e) | When the `anthropic` SDK is absent, `is_llm_available()` (env-key-only check) returned True, dispatch failed for every file (`RESULT.json: status=failed, files_written=[]`), yet `run_delivery` still emitted closeout `build_status=passed` — masked by the scaffold stub | Dispatch failure is now a **hard blocker** (`_generation_produced_real_files` / `_mark_generation_failed` in `delivery.py`); `is_llm_available` now requires an **importable provider SDK**, not just an env key | **e2e verified**: simulated dispatch failure fails-closed (`build_status=failed`, blocker recorded), no green off the stub | ✅ |
| 24 | **Repair loop non-convergence: the type module is excluded from repair scope** | `_build_filtered_generation` only scoped files tsc *named* (the `.tsx`), so `src/types.ts` — which must change to reconcile a component↔type mismatch — was never regenerated → whack-a-mole | On contract-mismatch errors, **`types.ts` now enters the repair scope** with the component's error as context (`_is_type_contract_failure` + `_type_module_path`); add-dependency action for missing known devDeps | **e2e verified**: real generation converged **tsc 18 → 0 in ONE cycle**; `tsc --noEmit` + `vite build` exit 0 (193 kB bundle) | ✅ |
| 26 | **Tests don't converge — test↔component *behavioral* contract mismatch** | Generated tests assumed UI state (form on mount) the component doesn't have; loop regenerated the test alone (never 0) | **Two-pass generation**: generate + build-verify the component source first, THEN generate each test against the FINAL on-disk source as ground truth (`test_two_pass_generation.py`) | **Mechanism ✅ verified** (`test_generated_against_source: TRUE`), BUT the real `claude-sonnet-4-5` e2e shows the two-pass mechanism does **not** make tests converge on a real product: **vitest 17/37**. Drift reduced, not killed | 🔄 **OPEN** — mechanism ✅, but tests do NOT pass e2e (see the "still open" section below) |
| 27 | **Generated `package.json` has mismatched Mantine sub-package versions** — vitest suites die at import (`SyntaxError: Named export 'use' not found … react is a CommonJS module`) | **Two compounding causes the first `9.4.1` pin missed** (caught by the real e2e): (a) `_MANTINE_VERSION` was `9.4.1` but **Mantine v9 requires React 19** while the react-vite template ships **React 18.3.1** → Mantine core imports React 19's `use` hook → the vitest SyntaxError; (b) the **remote build agent regenerates its OWN package.json** with skewed majors (core@9 + hooks/dates/charts@7) and `_merge_design_deps` only fills *missing* keys, so it never corrected the skew | Pin `_MANTINE_VERSION` to the **React-18-compatible v7 line (`7.13.2`)**; add the `@mantine/charts`→`recharts` peer on the Mantine branch; and a **force-normalization** pass (`enforce_dependency_versions` in `design.py` + `_enforce_design_deps` in `delivery.py`) that re-pins **every** `@mantine/*` (incl. strays the agent added) before the first validation **and inside every repair cycle** | test (`test_mantine_version_normalization.py`: React↔Mantine major-compat invariant, re-pin of a skewed package.json, on-disk rewrite; `test_product_design.py` consistency). **e2e NOT yet exercised** — the attempted re-verify used a Tailwind/shadcn profile (zero Mantine deps) and was aborted by Anthropic credit exhaustion; needs a Mantine-profile run once credits return | ✅ code / ⏳ e2e (billing-blocked) |
| 24b | **Type-contract drift — components disagree with `types.ts`** (repair loop non-convergent at scale) | The real e2e produced 13 tsc errors: `types.ts` declared `Expense.categoryId`/`date:string`/no `budget` but components used `expense.category`, `date:Date`, `Category.budget`. Root cause: **`types.ts` was LLM-generated** (a non-deterministic, contestable contract) and components received only the interface **names**, not the field-level schema — so every file invented its own fields, and the repair loop chased a **moving target** (fixing `types.ts` for one component broke another) | **Freeze the contract**: render `types.ts` **deterministically** (`_render_types`), **short-circuit it in the LLM `run_task`** (never LLM-owned — no token spent), **inject the full rendered source verbatim** into every component/test prompt ("use these EXACT field names and types; do not redefine"), and harden `_infer_ts_type` (booleans for status toggles like `reimbursed`, more numbers) so the frozen contract is *semantically* right. Repair now conforms components to a **fixed** target → converges | test (`test_single_file_prompt.py` #24b: contract injected verbatim, prompt==on-disk, no self-injection, boolean typing; `test_chunked_dispatch.py`: `types.ts` deterministic + zero LLM calls). **Partial e2e**: the freeze mechanism was confirmed live (types.ts was the ONLY file written before billing killed the run — via the deterministic short-circuit, no LLM call), but full build+vitest-green e2e is **blocked on Anthropic credits**. The same run exposed #29 (a garbled upstream extraction would freeze a garbage contract) — now fixed | ✅ code / ⏳ e2e (billing-blocked) |
| 25 | Illegal generated filenames from intent→file_spec (e.g. `Category;SeeRunningTotal.tsx` — a `;` from a workflow phrase) | intent→file_spec naming didn't have explicit coverage for illegal path chars, so intermittent workflow/entity phrases could survive into component filenames | `_to_pascal_case` / `_sanitize_component_name` now split on non-alphanumeric runs and drop leading digits before building file specs | test (`test_product_generation_manifest.py`), direct repro showing `CategorySeeRunningTotal` source/test paths and no semicolon path | ✅ |
| 28 | **Model discovery picks a NON-CHAT model** — OpenAI delivery 404s (`This is not a chat model … Did you mean v1/completions?`) with no explicit `SIGNALOS_LLM_MODEL` | `pick_best_model` picks the newest id by date; OpenAI's newest are `gpt-realtime-*`/`gpt-audio-*`/`gpt-4o-transcribe` (audio/realtime modalities) which won discovery then hard-404 on `/chat/completions` | Added `realtime`/`audio`/`transcribe`/`speech` to `_NON_FLAGSHIP_SUBSTRINGS` so discovery skips non-chat modalities and lands on a chat flagship. **Known remaining limit** (documented): responses-API-only flagships like `gpt-5-pro` still need an explicit pin — not solved by token-filtering | test (`test_model_discovery_chat_filter.py`), live probe (OpenAI key funded; `gpt-4o`/`gpt-4.1` return OK; discovery no longer returns `gpt-realtime-*`) | ✅ |
| 29 | **Deterministic intent extraction garbles entities** — "…category; running total; per-category breakdown; mark reimbursed" → ONE entity `Category;RunningTotal;Per-categoryBreakdown;MarkReimbursed` | `_extract_entities` split on `,`/`and` but **not `;`**, so a semicolon list merged into one blob; `_to_pascal_case` split on whitespace only, leaving `;`/`-` in the name. Normally masked by the LLM refine pass — but #24b now **freezes** the contract from these entities, so a garbled name = a syntactically invalid frozen `interface`, and offline/billing-blocked runs have no refine | Split entity lists on `;` too; `_to_pascal_case` now splits on any non-alphanumeric run + prefixes a leading digit → entity names are **always valid TS identifiers**. (Semantics — Title/Amount are really *fields* — remain the LLM refine's job; this is the deterministic floor) | test (`test_intent_entity_identifiers.py`: garbled prompts → valid identifiers, `;`-list split not merged, frozen `_render_types` interfaces valid), 74 intent tests pass, direct repro (entities now `Title/Amount/Category/RunningTotal/PerCategoryBreakdown/MarkReimbursed`, 0 invalid) | ✅ |
| — | **Flaky heartbeat test** under CPU contention (`test_background_heartbeat_fires_for_a_slow_task`: got 2 heartbeats, wanted ≥3) | A 0.25s task at a 0.05s interval left only ~2 heartbeat windows once a loaded runner starved the heartbeat thread — a timing calibration too tight, not a logic bug (passes 3/3 idle) | Widened the task to 0.6s (~12 windows) so the ≥3 assertion keeps a ~4× margin and still proves **periodic** firing | test (isolation 3/3), full suite 1806 passed pre-fix with only this 1 flake | ✅ |
| 30 | **Per-file `max_tokens` not clamped to the model ceiling** — OpenAI delivery hard-400s (`max_tokens is too large: 24000; model supports at most 16384`) | `_file_max_tokens` scaled a CRUD `.tsx` budget to 24000 and its docstring *claimed* it respected "the model's output ceiling" but never consulted the model; Claude tolerates it, gpt-4o rejects it | Clamp the budget to a per-model completion ceiling (`_model_max_output_tokens`, prefix-matched, conservative default 16384) at the call site — the model cap wins outright | test (`test_model_max_tokens.py`), surfaced + re-run via real OpenAI/gpt-4o delivery | ✅ |
| 31 | **`src/ui/*` scaffold files LLM-generated → hallucinated imports** — build fails: `Cannot find module '@mantine/core'` (in a shadcn app), `'react-jss'`, `'@fontsource/inter'`, `'./List'`/`'./Chart'`/`'./ExpenseManager'` | The LLM chunked path only short-circuited `types.ts` (#24b); the other deterministic boilerplate (`ui/theme.ts`, `ui/index.ts`, `AppLayout`, `PageLayout`, `product.css`, `setup.ts`) went to the model, which invented deps and barrels for files that never existed — same disease as #24b, one file-class over | Shared `_render_foundation_file` renders ALL scaffold/boilerplate files **deterministically** in both the local path AND the chunked-LLM `run_task`; only real product files (components, their tests, `App.tsx`) reach the model | test (144 gen tests; direct check: theme has no `@mantine`, AppLayout no `react-jss`), e2e: closure went **blocked→partial** (foundation builds clean) | ✅ |
| 32 | **LLM generates jest-style tests on a vitest stack** — build fails: `Cannot use namespace 'jest' as a value`, `Cannot find name 'describe'/'test'/'expect'`, unused `import React` | (a) tsconfig had no `types`, so `tsc` didn't know the vitest globals even with `globals:true`; (b) gpt-class models default to jest idioms (`jest.fn`, importing React) | tsconfig now loads `["vitest/globals","@testing-library/jest-dom"]`; the test prompt mandates vitest (`vi` not `jest`, globals not imported, no `import React`) | test (121 gen tests), manual patch of the e2e app proved the tsconfig fix clears all describe/test/expect errors | ✅ |
| 33 | **`sign_gate` raised on MIXED-role gates** — signing G3 with PO died on the PE-only `PLAN.md` (`role 'PO' is not authorised to sign 'core/execution/PLAN.md'`), breaking 3 `test_validate_wave_status` tests | #17 routed signing through role-enforcing `sign_gate`, which **raised per-artifact** on the first artifact a role couldn't sign — but a gate legitimately holds artifacts for different roles (PO signs its part, PE signs theirs, hence the test signs G3 twice). The docstring says raise only if the role can't sign *any* artifact (the union check) | `sign_gate` now **skips** artifacts the role isn't authorised for (union check still blocks a fully-wrong role like PO on QA-only G5); the wrong-signer-role test forges its bad signature via low-level `sign_artifact` so it still proves `validate_wave_status` is an independent check | test (`test_validate_wave_status.py` 6, `test_sign_cli_role_enforcement`, `test_sidecar_sign_role` — SoD intact), **caught by the full 2049-test backstop** | ✅ |
| — | **Env-fragile "without-LLM" tests** — 12 tests fail on a machine that exports a real `ANTHROPIC/OPENAI` key (a sourced `.env`); one captures `_LIVE = os.getenv(...)` at **import** time and an out-of-credit key then errors the live path | The offline suite assumed a key-free shell; keys leaking in from the ambient environment flipped `is_llm_available()` and broke deterministic assertions (pass in CI, fail locally) | New `python/conftest.py` clears provider keys **at conftest import AND via an autouse fixture** → the suite is hermetic regardless of the shell; live integration opts in with explicit keys | verified **green WITH fake keys exported** (all 15 backstop failures pass), 380 targeted tests green | ✅ |
| 34 | **Import allowlist hardcoded `@mantine`** — every product (incl. shadcn) was told it may import `@mantine/core`+`hooks`, so a shadcn app imported Mantine → build fails on the missing dep and the repair loop **burns cycles/tokens** chasing it (a "credit killer") | `_import_allowlist_lines` appended `@mantine/*` **unconditionally**, ignoring the selected design system | Allowlist is now **design-system-aware**: only the selected system's real deps (`@mantine/*` family for Mantine, `lucide-react`/`clsx`/`cva`/`tailwind-merge` for shadcn); the repair prompt is de-hardcoded too | test (shadcn allowlist has **no** `@mantine`; Mantine has the family) | ✅ |
| 35 | **#9 regressed on a REAL LLM run** — `claude-sonnet-4-5` generated an expense tracker that snapped onto the `financial-dashboard` blueprint again | The LLM got entities (`Expense`/`Category`) + workflows right but mislabeled `product_type` and set `ux_surfaces=[form,dashboard,list,chart]`. Keyword corroboration then matched finance on **2 generic tokens** — `dashboard` (a UI surface every app has) + `finance` (from `target_users`) — despite **zero** entity overlap | `_extract_intent_keywords` no longer draws from `ux_surfaces`/`product_type`; refined to keep **domain-specific** surfaces (`kanban`/`gantt`/`calendar`) so a "task board" still matches task-management while the generic ones can't leak | test (`test_blueprint_ux_surface_leak.py`); **real e2e**: expense → `blueprint:null` + builds green; task-board → task-management restored (the over-tighten was caught by CI) | ✅ |
| — | **CI gitleaks false-positive** on intentional fixtures — `test_secret_write_scan.py`'s fake `sk_live_/ghp_/AIzaSy` literals tripped the L0 secret scan and reddened the release | `.gitleaks.toml` allowlisted fixture files one-by-one and drifted; `scripts/test-gates.sh` already blanket-excludes `python/test_*.py` | Made `.gitleaks.toml` a faithful mirror (`python/test_.*\.py`) so a new fixture file can't recur the failure | CI green after fix | ✅ |
| — | **CI `test_secrets_resolver` fails on the runner** — `is_llm_available()` returned False with a valid product key | #23 made `is_llm_available` also require an **importable provider SDK**, absent on CI; passed locally (SDK present) | The tests exercise KEY RESOLUTION, so `_clear_provider_env` forces the SDK check True to isolate that concern | CI green | ✅ |
| — | **Flaky worktree race** — `test_isolate_true_uses_worktrees` intermittently `'failed' != 'completed'` (reproduced only with a stripped git config, i.e. CI) | The isolated parallel build ran the per-task cross-file check on **file-disjoint worktrees** (App.tsx in one task, a component in another) → falsely "unresolved import" depending on merge timing | Suppress the per-task cross-file check (`write_result=False`) for **both** parallel paths; cohesion is guaranteed by the #12 shared contract | verified **5/5** under a stripped git config (was flaky) | ✅ |

### Generation quality — mechanisms landed + a second funded e2e run (honest, `claude-sonnet-4-5`)

**Funded re-verification run (2026-07-04, task-tracker, `claude-sonnet-4-5`):** the generation mechanisms
hold in a real Claude run — **#36 propless CONFIRMED** (App renders `<Task />`, the component is
self-contained; zero prop drift), **#38 CONFIRMED** (tsc found zero balance/TS1005 syntax nits), the #24b
frozen types held (component used `{id,name}` exactly). It also surfaced concrete NEW issues the earlier run
had masked:

- **#40 (fixed):** the scaffold didn't ship `@testing-library/user-event`, which the test prompt permits and
  the model imported → TS2307 → **vitest collected ZERO tests**. After shipping it, vitest actually ran:
  **8 passed / 15**. tsc left only one trivial nit (unused `useState`, TS6133).
- **#41 (open, now the dominant blocker):** intent extraction reduced "each task has a **title, a priority,
  and a due date** … mark a task as **done**" to `Task { id, name }` — dropping priority/due-date/done. The
  thin entity yields a thin component (create+delete on `{id,name}`), but the prompt-informed test-writer
  still asserts done/priority behavior → most of the 7 vitest failures. Entity/field extraction quality is
  the next high-value fix, upstream of the operations contract.

The validation *inside* the delivery run failed for an **environmental** reason (npm install of Mantine's dep
tree exceeded the 300s validation cap → `tsc`/`vitest` not yet on PATH → the repair loop got "tsc not
recognized" instead of the real TS6133, so it couldn't fix the nit). The numbers above are from re-running
the real binaries by hand once install finished. **Not claiming green we haven't measured.**

| Gap | Evidence | Plan | Status |
|-----|----------|------|--------|
| **Component `Props` drift** (#36) | `App.tsx` passes props a component's `Props` interface doesn't declare (`Type '{ metrics… }' not assignable to DashboardSummaryProps`). The frozen contract (#24b) covers ENTITY types, not component prop shapes | **Eliminated the prop contract entirely** rather than freezing it: a *symmetric PROPLESS* rule — App's prompt renders every component `<Xyz />` (never `<Xyz items={…}/>`, no `Props` for children); each component's prompt must be self-contained and declare **no required props** (a required prop would be `undefined` at App's propless call site → tsc rejects it). Nothing passed + nothing required ⇒ no prop shape to drift. Enforced by tsc (drift → `Property 'x' does not exist on IntrinsicAttributes` / `is missing` → repair regenerates with the same rule → converges); the deterministic path (`_render_app`) was already propless. 3 unit tests; 59 green | ✅ |
| **Tests don't converge (vitest)** (#37) | real e2e **17/37**; `User.test.tsx` asserts edit/update/cancel the component doesn't implement (#26 not converged at product scale) | **Shared deterministic OPERATIONS contract** (the #24b pattern one layer over): `_operations_contract(entity, fields)` derives ONE authoritative op list — CREATE, DELETE, TOGGLE per boolean field — injected verbatim into BOTH the component prompt ("implement EXACTLY these") and the test prompt ("assert EXACTLY these, and NOTHING beyond them; do NOT test edit/update/cancel"). Inline edit/update **excluded on purpose** — it was the exact source of the `edit/update/cancel` drift and is the flakiest flow to generate; create+delete+toggle is a coherent, honest CRUD-lite. Mechanism landed + **6 unit tests**. Funded run: contract mechanism is correct, but it is **bottlenecked upstream by #41** (a thin `{id,name}` entity gave the contract only create+delete, yet the prompt-informed test-writer still asserted done/priority) → vitest **8/15**. Fixing #41 is the lever, not the contract | 🔄 mechanism ✅, gated by #41 |
| **Repair loop misses syntax nits** (#38) | a 1-char malformed `describe(...)` close (`}` vs `});`, TS1005) needed a HAND-fix; the repair loop didn't converge on it (TS1005 *was* parsed, but tsc's column points DOWNSTREAM of the real imbalance, so the model re-made the same nit) | **Deterministic delimiter-balance pass** `analyze_delimiter_balance()` (string / template-literal / comment aware) enriches any tsc syntax-class failure (TS1005/TS1128/TS1109/TS17002/…) with a crisp, correctly-localized `BALANCE` diagnostic — *"N unclosed `(` — a `describe`/`it`/`expect` closed with `}` instead of `});`; re-emit the WHOLE file"*. **Corroboration-gated**: only fires alongside a real tsc syntax error. 10 unit tests. **Funded run CONFIRMED**: tsc found zero syntax/balance errors in the generated component + test — the class of nit this targets did not recur | ✅ (confirmed in funded run) |
| **Scaffold missing `user-event`** (#40) | funded run: the test prompt permits `userEvent`, the model imported `@testing-library/user-event`, but the scaffold shipped only react/dom/jest-dom → TS2307 → **vitest collected 0 tests** (masked the real pass rate entirely) | Ship `@testing-library/user-event` in `_PACKAGE_JSON_TEMPLATE` devDeps + add it to the test import allowlist (the repair loop already knew to add it on TS2307; shipping upfront is the right default). After the fix vitest ran 15 tests (8 passed). 1 regression test | ✅ |
| **Entity/field extraction too lossy** (#41) | funded run: "each task has a **title, a priority, a due date** … mark a task as **done**" → `Task { id, name }` (dropped priority/due-date/done). Thin entity ⇒ thin component (create+delete on `{id,name}`), but the prompt-informed test-writer asserts done/priority ⇒ most of the 7 vitest failures | Harden intent field-extraction: `_extract_entity_fields` DISCOVERS the subject of "`<det> <subject> has <list>`" as the entity + the list as its fields; "mark `<subject>` as `<status>`" → a boolean field. Reconciles the entity list (drops field tokens mis-listed as entities). Unambiguous has-verbs only (not records/tracks — noun homographs). 5 tests. **Funded RE-RUN CONFIRMED**: same prompt now yields `Task {id, title, priority, due_date, done: boolean}` (was `{id,name}`), and the design agent's own reasoning cited the richer fields | ✅ (confirmed in funded re-run) |
| **Scaffold missing `matchMedia`/`ResizeObserver`** (#42) | funded re-run: with #41's richer entity the model correctly used **Mantine** components, but jsdom implements neither `window.matchMedia` nor `ResizeObserver` (Mantine's hooks call them on render) → every render threw → **all 9 tests failed at render** | Stub both in the generated vitest setup (`_render_vitest_setup` + scaffold `_VITEST_SETUP`) — standard Mantine+jsdom boilerplate, guarded/harmless for libs that don't use them. Proof: after the fix renders stopped crashing (vitest 0 → 2 passed). 1 regression test | ✅ |
| **Ambiguous test queries** (#43) | funded re-run: after #42 the remaining 7/9 failures were all one cause — `getByLabelText(/priority/i)` matches BOTH the form label AND "Priority" shown in the task list → "found multiple elements" | Test prompt now demands UNAMBIGUOUS queries: form controls by role + an ANCHORED exact name (`getByRole('textbox', { name: /^title$/i })`), scope row-vs-form with `within(...)`, prefer `findBy*` after an interaction. 1 unit test. **3rd funded run CONFIRMED**: the generated test now uses exactly those anchored/`within`/`findBy` patterns (no bare `/priority/i`) | ✅ (confirmed in 3rd run) |
| **Tests don't isolate (store leak)** (#45) | 3rd funded run: tsc CLEAN + renders work, but vitest **3/10** — ~all 7 failures were "found multiple elements with role button name /delete task/i". Cause: the test had NO `beforeEach` and the component's zustand store was a NON-exported module singleton → state accumulated across tests → every later query matched one control PER row | Component EXPORTS its module store; test resets it in `beforeEach`. **4th run exposed my mistake**: shipping the store's *location* as prompt guidance (not a contract) made the App test invent a phantom `./store/taskStore` → superseded by **#46/#47/#48** | ↻ superseded by #46–#48 |
| **#45 done right + repair hardened** (#46/#47) | 4th funded run: #45 as prompt-guidance REGRESSED the build — the App shell test invented `./store/taskStore` (TS2307) the repair loop couldn't converge; a cross-file dependency shipped as prose, not a contract | **#46** differentiate isolation by file: the component test resets its OWN store (imported from the same module, seen in the #26 ground truth); the App test owns no store → isolate via unmount, forbidden from `./store/*`; scope rows via `getAllByRole('listitem')[i]` (HTMLElement, kills the `.closest('li')` TS2345). **#47** `_import_drift_enrichment`: any TS2307 now feeds the repair loop the real manifest paths so it removes the phantom instead of re-inventing it. 5 tests. **5th run CONFIRMED**: component test **6/6 green** | ✅ |
| **App shell test drifts (deterministic fix)** (#48) | across every funded run the App-shell TEST was the perennial drift point — prop drift, phantom store import, then (run 5) **29 malformed-JSX tsc errors** that failed the build while the component test passed 6/6. App is a composer; its test needs zero LLM creativity | Render `src/App.test.tsx` deterministically (joins the #31 foundation set) as a minimal composition SMOKE — `render(<App/>)`; `expect(container.firstChild).toBeTruthy()` — no content assumptions, cannot drift. App.tsx + real components + their tests stay LLM. **PROVEN against run 5's real repo**: swapping it in took the repo from "29 tsc errors, build failed" to **tsc clean (1 repair-fixable nit)** + **vitest 7/7 (both files pass)**. 1 test | ✅ (proven 7/7) |
| **UI-library hardcoded to a pair** (#44) | the design system was 2 hardcoded options repeated in 5 places (LLM prompt, validator, heuristic, deps, import allowlist) — unextendable, and the founder had no say (agent proposed AND committed silently, violating the signed-stack "A proposes, B signs" rule) | **Extensible `UILibraryAdapter` registry** in `design.py` = single source of truth; one entry carries name/version/prompt-desc/deps/import-packages/`fit`. Registering an adapter wires it EVERYWHERE (validator, prompt, heuristic, deps, allowlist). Adding MUI/Chakra = one entry, not 5 edits (proven by a Chakra test flowing through end-to-end). Existing two byte-preserved. 6 tests. (Founder-DECLARED selection is the next step.) | ✅ |
| **Breadth** (#39) | proven on ONE product type / ONE prompt (expense tracker, react-vite) | **Deterministic breadth harness** — 6 diverse products (expense / task-board / CRM / habit / inventory / bookmarks) driven through the REAL `build_generation_packet` + deterministic `_render_react_vite_files`; asserts the invariants hold **per product**: types.ts names every entity, App renders each component **propless** (the #36 invariant, structurally guaranteed here), foundation files present, every component+test renders, and the #37 operations contract derives (create+delete always, toggle per boolean, never edit). **4 tests, 24 subtests**. The LLM-path matrix (correct component BODIES per domain) still needs a credit-funded run | 🔄 deterministic ✅, LLM-path pending |

**The through-line (confirmed by the e2e):** chunking small fixed truncation and shells, but generating
each file **independently** dropped the *connection* — exactly the "small enough **and** keep everything
connected" principle. #12 restores cohesion via a shared, authoritative cross-file contract.

**Architectural through-line (#7):** the *planning* layer already decomposes work into small, linked, traceable
units (acceptance criteria → `T-00N` tasks → `file_specs` tagged with `acceptance_id`/`task_id`), but the
*execution* layer discarded that and dumped everything into one call. The fix makes generation **execute the
plan's decomposition** — small enough + kept connected — rather than flatten it.

---

## Option 2 — containerized build/validation (✅ landed on main)

A capability, not a bug-fix: build/validate **any** stack in an on-demand container so the operator
installs **no language toolchain** — the answer to "we're always one language short" and "assume nothing
local." **Proven:** a Go app `go vet`'d + compiled to a binary with **zero Go on the host** (ran in a
`golang` container).

**Landed on main** (was on `feat/option2-containerized-validation`, which went stale as main was rewritten):
the runtime-agnostic sandbox (`sandbox.py`, already wired into the harness/e2e/tdd runners) + its
consent-gated Podman auto-install came across as a clean forward-evolution, and `validation._run_commands`
now routes the build/test commands through the container shim. Wiring, runtime detection, image resolution,
and the install plan are **test-covered offline** (46 sandbox + 5 validation-shim tests, no real runtime
needed); the default path (host tool present) is byte-unchanged. End-to-end container execution still needs
a runtime on the machine to exercise live (proven earlier on the branch with Go).

| # | Item | What it does | Status |
|---|------|--------------|--------|
| O2-1 | **Not-installed → container shim** | `validation._run_commands`: host tool present → use it; **not installed → `docker run` in the per-stack image**; neither → honest "need Docker" blocker | ✅ (proven: Go builds with no host Go) |
| O2-2 | **Runtime-agnostic + floating images** | detect `docker`\|`podman`; images are **floating tags** (`golang:1`, `node:lts`, `dotnet/sdk:latest`) the registry resolves to the **latest at pull time** — never a platform-hardcoded minor, never the model's cutoff-guessed version | ✅ (34 sandbox tests) |
| O2-3 | **Consent-gated Podman auto-install** | if no runtime, install a **license-clean rootless** runtime (Podman, not licensed Docker Desktop) — **only with explicit consent**; never silent. Per-OS plan (winget/brew+machine/apt\|dnf) | ✅ lib + tests / ⏳ first-run UI+IPC wiring |
| O2-4 | **Windows `-v` mount fix** | native `C:\` path mangled the Docker mount → container saw no source → empty-output "fail". Normalized to forward slashes (no-op on Linux) | ✅ |
| O2-5 | Fleet fallback (remote when no local Docker); route `build_validation_plan` through the shim for every profile; Option-1 app-carried Dockerfile for the tail; **generation-side "fetch latest deps, don't guess from cutoff"** (the `_MANTINE_VERSION` hardcode) | the "assume nothing local" end-state + applying the same principle to what the agent *writes* | ⏳ planned |

**Design decision recorded:** local runtime auto-install = **$0-per-build** (compute on the user's machine);
the **fleet** (remote managed compute) is the **optional paid tier** for "no local runtime + won't install."
Bundling a runtime+VM was rejected (heavy app + a VM image to maintain) unless offline-first-launch is required.

---

## Wiring audit — declared-but-dead governance (full report: `docs/WIRING_AUDIT.md`)

A full audit of **every hook, agent, role, and enforcement rule** (3 parallel auditors, `file:line`
evidence). Headline: only a thin spine is truly live (harness step-hooks, the 6 G0–G5 gate agents,
and 4 of 12 rules enforced in `agent_loop.py`). The rest is produced-then-ignored, plugin-only, or
no-op — which directly contradicts the "fully wired & enforced, never advisory" promise. Each row
below is now a tracked fix with an exact wire-in location.

| # | Gap | Why it's dead | Where it must be wired | Severity | Status |
|---|-----|---------------|------------------------|----------|--------|
| 14 | **Architect/plan contract never reaches code generation** (signed, then ignored) | `arch_review` + `design_decisions` are built/validated/signed (`delivery.py:431-474`) but `build_generation_packet` had no arch/plan param — the generator built off blueprint/intent and ignored the architecture it just signed | Delivery now passes the real signed `ARCH_REVIEW.yaml`, `DESIGN_DECISIONS.yaml`, and `SCOPE_DECISIONS.yaml` into `prepare_generation`/`build_generation_packet`; the packet stores `generation_contracts`; file specs carry binding constraints; monolithic and single-file prompts render `Binding Product Contracts` | 🔴 Critical | ✅ verified: `test_product_generation_manifest.py`, `test_product_delivery_e2e.py -k generation_packet_carries_signed_contracts`, `test_single_file_prompt.py` |
| 15 | **UI rule modes silently discarded in production** | `_build_agent_enforcement` returned `None`; the Rust `EnforcementStore` the UI edits was in-memory-only, never persisted, never read by the Python sidecar | Rust `set_rule_mode` now **persists to `.signalos/enforcement.json`** (loaded at startup); Python `_build_agent_enforcement` returns a real `FileEnforcementProvider` that reads it, applying the **core-invariant floor** (a core rule reads strict even if config says off) | **e2e verified**: persisted `stack-contract:off` reads off; `trust-tier:off` forced strict; live loop denies/allows per config. 112 py + 88 Rust tests pass | ✅ |
| 16 | **6 of 12 enforcement rules are pure no-ops** — `plan-gating`, `audit-append`, `stack-contract`, `gate-compliance`, `zero-manual-regression`, `mutation-threshold` | Declared + toggleable, read by no enforcement path | Wired to **real denials**: plan-gating + audit-append in `agent_loop._check_governance`; stack-contract + gate-compliance + zero-manual + mutation-threshold in Rust `precheck_rules` (guarded by mode) | **verified**: block-and-relax test pairs for all; `wave-freeze` is now separately live-verified in #18 | ✅ |
| 17 | **Role-gating bypassed on the live manual sign path** — a PO could sign G5 (a QA gate) | Sidecar hardcoded `--role PO` + used `sign_artifact` (no role check), never `sign.sign_gate`; `role_can_sign` only advisory in JS | `ipc.rs sign_gate` + Python `sign_gate` now pass the **real identity role** and route through `sign.sign_gate` (role-enforcing) | **live-verified**: PO signing G5 → `ValueError "role 'PO' is not authorised… (required: ['QA'])"`, no signature written; QA signs G5; PE can't sign G1 | ✅ |
| 18 | **wave-freeze is a flag nothing enforces** | `freeze_wave`/`unfreeze_wave` were UI-wired, but the only consumer `build_precheck` had **no caller in `src/`** — freezing set a state no build read | `build_precheck` now supports a rule filter, and both live build entrypoints call the `wave-freeze` precheck before dispatch: chat delivery (`agent:deliver`) and approved-plan/retry wave dispatch (`signal-orchestrate`). Frozen waves block with the existing plain reason instead of starting Build. | 🟠 Med | ✅ verified: Rust `wave_freeze_filter*` tests, `chat.freeze-consolidation.test.ts`, `approvePlan.precheck.test.ts`, `npm run typecheck` |
| 19 | **Two dead hooks + one mis-registered + one inert** | `pre-deploy`/`post-retro` had real scripts with zero product runtime callers; `pre-session-compress` was registered under a non-existent Claude event; `step-pause-check.sh` needed `SIGNALOS_PLAN_STEP_JSON` from the harness | `signal-land-deploy` now runs `pre-deploy` fail-closed before deploy mutation; new `signal-post-retro` CLI/IPC/bundled command runs `post-retro` fail-closed; `_fire_hook` exports `SIGNALOS_PLAN_STEP_JSON` for `step-started`; `pre-session-compress` is reclassified reference-only because `context.py` enforces the invariant in-product | 🟠 Med | ✅ verified: `python/test_deploy_hook_wiring.py`, `python/test_post_retro_hook_wiring.py`, `python/test_harness_hooks.py` |
| 20 | **Plugin-only hooks masquerade as product enforcement** | `session-start`, `pre-commit`, `pre-merge`, `pre-tool-use-guard.sh` (secret-scan + T3 write block) fire **only** under the Claude/Cursor plugin — SignalOS's own runtime invokes none, no product-side fallback | Highest-severity slice **done via #20a**: `pre-tool-use-guard.sh`'s two dimensions now have live product-runtime analogues in `agent_loop` — T3/path block (trust-tier allowlist) + content secret-scan (`secret-block`, audited `denied`). Broader `session-start`/`pre-commit`/`pre-merge` plugin-vs-runtime parity still open | 🟠 Med | 🔶 partial (write-content slice via #20a; session/commit/merge parity open) |
| 20a | **DATA-LEAK HOLE: write-content secret scan is plugin-only** — an agent can hardcode a secret (`sk-ant-…`) into a generated source file and the product runtime won't catch it | Logs are redacted (`redact.py`) and writes *to* `.env` are blocked (`secret-block`), but scanning write *content* for embedded secrets lived only in the plugin-only `pre-tool-use-guard.sh`/`pre-commit`. `agent_loop._scan_write_content` scanned injection only, and just warned | Product runtime write/edit now loads the canonical redactor and **blocks** secret-like content under `secret-block` before writing (denials audited as `denied`). **Follow-up fix (must-have):** the first cut used *name-only* patterns that false-blocked benign code (`const token = getToken()`, `sessionToken`, `password: userVar`) — which would break generation. Replaced with a **value-aware** heuristic (`_WRITE_SECRET_VALUE_RE` + `_looks_like_secret_literal`): blocks a secret-named var assigned a quoted, secret-looking *literal* only | test (`test_secret_write_scan.py` 0 false-pos / 0 missed; `test_product_agent_loop` incl. `STRIPE_SECRET_KEY="plaintext"` denied; **85 tests pass**), repro `DENIED`, file not materialized | ✅ |
| 21 | **The Test + Review agents are never dispatched — the missing generation quality gate** (re-severitized from Low: **this is a root cause of the broken generation output**) | The architecture specifies **Build → Test → Review** (`test.md`: *"run the full suite against every Build PR before Review"*; `review.md`: *"spec-drift + correctness + test-evidence check … before merge, on every PR"*). But in the delivery path only Build runs — `test.md`/`review.md` are handed to the Build agent as reference `applicable_skills` (`generation.py:80/89`), **never dispatched to check the output**. So generated code ships unbuilt, untested, unreviewed — exactly the "shit output". The compile-feedback repair loop (#22) is only a *mechanical stand-in* for the Test agent | **Test** is now the validation phase (real `tsc`+`vite build`+`vitest`, fail-closed via #23); **Review** is a new deterministic gate `review_gate.py` (`run_review_gate`) wired as phase 6c in `delivery.py` after Build+Test, before closeout. It checks **spec-coverage** (every entity has a component; gross "entities but zero components" → block), **test-evidence** (every component has a sibling test that renders + asserts — render-only smoke accepted, missing/empty test → block), and **build-correctness** (a `failed` build → block). Governed by the **`gate-compliance`** core-invariant rule: `strict` → a blocking verdict fails the closeout **closed** (`closure_level`→partial + blocker); `warn` → records limitations without failing closed; `off` impossible (core floor). Verdict persisted to `REVIEW_RESULT.json` | 🔴 **High** | ✅ code (`test_review_gate.py` 10 tests: block on missing/empty test, gross spec gap, failed build; warn-mode records-not-blocks; render-only accepted; delivery e2e regression running) |
| 21b | Remaining undispatched agents: `security`/`release`/`worktree-sync` `.md` (real logic lives in `security_gate.py`/etc.); `backlog-harvester`/`metrics-adapter`/`qa-evidence-pack` `.sh` (zero invocation) | Shape-validated docs / utilities, not the quality gate | dispatch or reclassify as reference (stop presenting as live agents) | 🟡 Low | ⏳ |

**Meta-finding:** #15 is the amplifier — because the production enforcement provider is `None`, *nothing*
the UI toggles reaches the agent loop. Fixing it makes the 4 real rules honor config **and** is the
precondition for the 6 no-op rules (#16) to mean anything once wired. Fix order: **15 → 16 → 17**, with
#14 is now handled by the #12 cohesion work: signed architecture, design, and scope contracts reach generation packets, file specs, and build prompts.

---

## Earlier this session — governance/enforcement integrity (shipped in v3.2.0-internal.1)

Summarized; see `CHANGELOG.md` and `docs/GOVERNED_PRODUCT_LIFECYCLE_IMPLEMENTATION_PLAN.md` for detail.

| Area | Issue fixed |
|------|-------------|
| Gate signing | Was fail-**open** when required artifacts missing/placeholder → now **fail-closed** |
| Audit trail | Not tamper-evident → **hash-chained** and wired to `integrity-witness` |
| Enforcement UI | Read wrong backend field (`.rules` vs `.modes`) → always "No rules loaded"; fixed |
| Enforcement toggles | Core invariants were toggleable off → `set_rule_mode` now refuses to disable core invariants |
| Dormant QA | `ux_friction` agentic QA never ran → wired into the G3 design-gate preview |
| Belief resolution | Built a duplicate module → rewired the signed-threshold gate into the **real** live `evaluate_listening_window` |

---

## Full-spectrum governance audit — epoch-1 (critical fail-open closure)

An external full-spectrum audit claimed Foundry has contradictory control planes and gates that approve missing/stale/unverified work. **Independently verified** by 6 adversarial read-only agents with 8 `$0` reproductions against the current tree: **substantially CONFIRMED — ~18 genuine fail-open bugs.** Corrections in our favor: BLOCKED tasks correctly fail *red* (audit wrong there); the "wiring wall always passes" is our *deliberate* advisory demotion; benchmark-default profile is intentional (real gap = desktop inheriting it). Fixed in 4 workstreams, each **RED→GREEN** (a repro that passes against the buggy code, flipped by the fix). Full suite **2839 passed**.

| # | Confirmed fail-open hole | Fix | Verified |
|---|---|---|---|
| G-1 | **Signatures forgeable** — any `signer:` text = signed on status/wave_engine/preflight; a `role:HACKER verdict:REJECTED` no-hash block read as SIGNED | ONE strict validator `sign.check_gate_signed_strict` (APPROVED verdict + authorized role + valid current hash + audit-chain + non-revoked, + durable revocation marker) wired into status/wave_engine/preflight (the primary gating surfaces). ship.py/release_readiness already check hash+draft+agent-signer; converging them onto the strict validator is deferred (their ready-fixtures need strict-compliant signatures) | ✅ `test_gate_signature_forgery.py` (forged/rejected/hashless/wrong-role/revoked → NOT signed); 378 pass |
| G-2 | **`python -c` escapes write-governance** — gutted a signed test, wrote secrets, tampered the audit trail while `write_file` denied it; no post-command diff | `run_command` writes now go through path/spec-immutable/`.signalos`/secret governance + a **post-command FS-diff** (scoped to product source): forbidden writes reverted + denied + audited (content-hashed) | ✅ `test_agent_loop_containment_holes.py` (real `python -c` denied+reverted+audited) |
| G-3 | **G4 verifier agent-controlled + run-blind** — model rewrote `package.json`/`vitest.config` to neuter the exam; G4 never read `agent_result` so stale-green passed | G4 verification contract (`scripts.test`/`build` + test-discovery) **frozen/immutable**; orchestrator consumes `LoopResult` (refused/stalled/no-file → gate parked, no review); + a `run_validation` frozen-tests-collected backstop | ✅ containment + gate_orchestrator suites |
| G-4 | **G0 1-of-4 / G2 Expectation-Map-only** signed vacuously | G0 requires ALL manifest artifacts; G2 requires the full plan contract (`PLAN.tasks.yaml` + acceptance + RED skeletons + rendered-plan parity) | ✅ `test_product_gate_orchestrator.py` |
| G-5 | **No G5 verifier + push ships nothing** — approve immediately completes; `ready`=`len(waived)==0`; `git push HEAD` with no add/commit | deterministic G5 release verifier (release proof / no unresolved conditions / runtime proof); `git add -A`+`commit` **before** push | ✅ gate_orchestrator suite (commit count 1→2) |
| G-6 | **Signed artifacts mutable**; blank waivers; approve-with-conditions unconditional; sign-before-persist | signed `core/**` **immutable**; waivers need a reason; unresolved conditions block `ready`; persist-before-dispatch (errors surfaced) | ✅ containment + orchestrator suites |
| G-7 | **Reviewer not a hard wall + write-capable**; imperative follow-ups ("fix it") lost to write-forbidden chat | reviewer re-runs to PASS (green tests can't override); reviewers get a **read-only tool set**; context-aware intent routing (frontend + backend) sends edits to the build path | ✅ `test_subagent_build.py`, `test_project_ipc.py`, `chat.delivery-intent.test.ts` |
| G-8 | **wave-freeze UI-only; warn==strict; `.signalos/` protection never fired** (latent) | freeze enforced before every tool dispatch; observe/warn/block semantics (warn logs+allows); `.signalos/` path-normalization bug fixed | ✅ containment suite |

**Deferred to epoch 2** (need bigger infra; documented in-code as `# INTEGRATE`): one canonical state machine (kill the 3 control planes), authenticated actor / separation-of-duties (one signer currently signs PO+PE+QA), full transactional CAS + tenant binding, evidence bound to commit/tree hash, desktop-defaults-to-production + resume-persists-profile/verify/signer.
